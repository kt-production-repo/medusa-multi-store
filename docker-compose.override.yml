# Local-only smoke-test override — NEVER commit. Publishes the internal-only
# ports so tests can hit the API/storefront from the host. Auto-loaded by
# docker compose alongside docker-compose.yml.
services:
  backend-server:
    ports:
      - "9000:9000"
  storefront:
    ports:
      - "8000:8000"